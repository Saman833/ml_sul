# Windows-compatible versions for LoRA training
# These versions are tested to work together on Windows

# Core ML libraries - standard versions (CPU optimized)
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
transformers==4.35.2
datasets==2.14.7
peft==0.6.2
accelerate==0.24.1
tokenizers==0.15.0

# Data processing
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0

# MLflow for experiment tracking
mlflow>=2.5.0

# Jupyter and development
jupyter>=1.0.0
ipykernel>=6.0.0
tqdm>=4.65.0

# Additional utilities
requests>=2.28.0
huggingface-hub>=0.17.0

# Note: This will install the latest compatible PyTorch version
# Standard PyTorch includes both CPU and GPU support automatically
